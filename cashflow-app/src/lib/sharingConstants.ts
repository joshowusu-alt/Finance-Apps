/**
 * Client-safe constants for the household sharing feature.
 * Keep this file free of server-only imports (crypto, db, etc.).
 */

/** localStorage key where the partner's join token is stored on their device. */
export const CF_JOIN_TOKEN_KEY = "cf_join_token";
